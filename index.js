"use strict";
  let y = 0; 
  let arr = [-5, -3, 5, 10]; 
  let summ = 0; 
  for (let i = 0; i < arr.length; i++) { 
   if (y > arr[i]) { 
   } else { 
   summ = summ + arr[i]; 
   } 
  } 
  console.log(summ);